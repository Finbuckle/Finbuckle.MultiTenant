name: Release

on: [ workflow_dispatch ]

permissions:
  contents: read

jobs:
  legacy-release-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Explain release workflow
        run: echo "Use Draft Release for current releases. Select 8.x or 9.x for legacy semantic-release."
